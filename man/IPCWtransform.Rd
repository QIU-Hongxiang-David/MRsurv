% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/IPCWreg.R, R/IPWreg.R
\name{IPCWtransform}
\alias{IPCWtransform}
\title{Inverse-probability weighting (IPCW) transformation}
\usage{
IPCWtransform(
  follow.up.time,
  pred_censor_obj,
  tvals,
  next.visit.time = Inf,
  id.var,
  time.var,
  event.var,
  denom.survival.trunc
)

IPCWtransform(
  follow.up.time,
  pred_censor_obj,
  tvals,
  next.visit.time = Inf,
  id.var,
  time.var,
  event.var,
  denom.survival.trunc
)
}
\arguments{
\item{follow.up.time}{see \code{\link{MRsurv}}}

\item{pred_censor_obj}{a \code{pred_surv} object for time to censoring in the time window of interest}

\item{tvals}{see \code{\link{MRsurv}}.}

\item{next.visit.time}{the next visit time. Default is \code{Inf}, corresponding to the last time window}

\item{id.var}{see \code{\link{MRsurv}}}

\item{time.var}{see \code{\link{MRsurv}}}

\item{event.var}{see \code{\link{MRsurv}}}

\item{denom.survival.trunc}{see \code{\link{MRsurv}}}
}
\value{
a named one-column matrix of transformations used for regression. Each row corresponds to an individual. Row names are elements in \code{follow.up.time$id.var}

a named one-column matrix of transformations used for regression. Each row corresponds to an individual. Row names are elements in \code{follow.up.time$id.var}
}
\description{
Given a \code{pred_surv} object for time to censoring in a time window, calculates the IPCW transformation in the time window. The transformation is used as the outcome when estimating the conditional survival probability at the next visit time.

Given a \code{pred_surv} object for time to censoring in a time window, calculates the IPCW transformation in the time window. The transformation is used as the outcome when estimating the conditional survival probability at the next visit time.
}
\section{Warning}{

This function is designed to be called by other functions such as \code{\link{IPCWsurv}}, therefore inputs are not thoroughly checked. Incorrect inputs may lead to errors with non-informative messages. The user may call this function if more flexibility is desired.


This function is designed to be called by other functions such as \code{\link{IPCWsurv}}, therefore inputs are not thoroughly checked. Incorrect inputs may lead to errors with non-informative messages. The user may call this function if more flexibility is desired.
}

