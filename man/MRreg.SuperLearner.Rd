% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MRreg.R
\name{MRreg.SuperLearner}
\alias{MRreg.SuperLearner}
\title{Regression based on multiply (sequentially) robust transformation of fitted survival and censoring probabilities using SuperLearner}
\usage{
MRreg.SuperLearner(
  covariates,
  follow.up.time,
  pred_event_censor.list,
  visit.times,
  tvals,
  truncation.index,
  id.var,
  time.var,
  event.var,
  U.formula = NULL,
  Q.formula = ~.,
  U.SuperLearner.control = list(family = gaussian(), SL.library = "SL.lm"),
  Q.SuperLearner.control = U.SuperLearner.control,
  U.folds,
  cluster.var = NULL,
  obs.weight.var = NULL,
  corstr = "independence",
  denom.survival.trunc = 0.001
)
}
\arguments{
\item{covariates}{see \code{\link{MRsurv}}}

\item{follow.up.time}{see \code{\link{MRsurv}}}

\item{pred_event_censor.list}{list of \code{pred_event_censor} objects (see \code{\link{pred_event_censor}}). Each \code{pred_event_censor} object in the list corresponds to a time window in \code{visit.times} after \code{truncation.index} in increasing order.}

\item{visit.times}{see \code{\link{MRsurv}}}

\item{tvals}{see \code{\link{MRsurv}}}

\item{truncation.index}{see \code{\link{MRsurv}}}

\item{id.var}{see \code{\link{MRsurv}}}

\item{time.var}{see \code{\link{MRsurv}}}

\item{event.var}{see \code{\link{MRsurv}}}

\item{U.formula}{see \code{\link{MRsurv}}}

\item{Q.formula}{see \code{\link{MRsurv}}}

\item{U.SuperLearner.control}{see \code{\link{MRsurv}}}

\item{Q.SuperLearner.control}{see \code{\link{MRsurv}}}

\item{U.folds}{a list of vectors of id (identified by variable \code{id.var}) corresponding to each fold for cross-fitting. Set to a list containing one vector for no cross-fitting.}

\item{cluster.var}{see \code{\link{MRsurv}}. If provided, this variable is used to split samples when using cross-fitting and/or cross-valiation, as well as inference of marginal survival probability.}

\item{obs.weight.var}{see \code{\link{MRsurv}}}

\item{corstr}{see \code{\link{MRsurv}}}

\item{denom.survival.trunc}{see \code{\link{MRsurv}}}
}
\value{
a list of \code{SuperLearner} models (conditional probability) or \code{\link{intercept_IF_model}} objects (marginal probability) corresponding to \code{tvals}.
}
\description{
Apply sequentially doubly robust transformation on fitted survival and censoring probabilities in each time window and estimate P(T > t | T > tk, covariates available at tk) with \code{\link[SuperLearner:SuperLearner]{SuperLearner::SuperLearner}}.
}
\section{Warning}{

This function is designed to be called by other functions such as \code{\link{MRsurv}}, therefore inputs are not thoroughly checked. Incorrect inputs may lead to errors with non-informative messages. The user may call this function if more flexibility is desired.
}

\section{Custom learners}{

Custom learners may be specified by providing an element named \code{SL.library} in \code{Q.SuperLearner.control}.The user may refer to resources such as \url{https://cran.r-project.org/web/packages/SuperLearner/vignettes/Guide-to-SuperLearner.html} for a guide to create custom learners.
}

