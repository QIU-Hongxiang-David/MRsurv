% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_surv.R
\name{fit_rpart}
\alias{fit_rpart}
\title{Wrapper of \code{rpart::rpart} and \code{partykit::as.party}}
\usage{
fit_rpart(
  formula,
  data,
  id.var,
  time.var,
  event.var,
  nfold = 2,
  time.grid = NULL,
  time.grid.size = 250,
  option = list(),
  cluster.var = NULL,
  obs.weight.var,
  ...
)
}
\arguments{
\item{formula}{formula used by \code{\link[rpart:rpart]{rpart::rpart}}}

\item{data}{data containing all covariates, follow-up time, event indicator and id}

\item{id.var}{see \code{\link{MRsurv}}}

\item{time.var}{see \code{\link{MRsurv}}}

\item{event.var}{see \code{\link{MRsurv}}}

\item{nfold}{number of folds used when fitting survival curves with sample splitting. Default is 2. If \code{nfold=1}, sample is not split.}

\item{time.grid}{a vector of time points at which to evaluate the survival curve. If NULL, a default grid will be used.}

\item{time.grid.size}{size of time grid, default to 250. If \code{time.grid} is provided (not NULL), this argument will be ignored for all survival nuisance estimators.}

\item{option}{a list containing optional arguments passed to \code{\link[rpart:rpart]{rpart::rpart}}. We encourage using a named list. Will be passed to \code{\link[rpart:rpart]{rpart::rpart}} by running a command like \code{do.call(rpart, option)}. The user should not specify \code{formula} and \code{data}.}

\item{cluster.var}{see \code{\link{MRsurv}}. If provided, this variable is used to split samples when using cross-fitting.}

\item{obs.weight.var}{see \code{\link{MRsurv}}}

\item{...}{ignored}
}
\value{
a \code{\link{pred_surv}} class containing fitted survival curves for individuals in \code{data}
}
\description{
First use \code{\link[rpart:rpart]{rpart::rpart}} to obtain a tree and then use \code{\link[partykit:party-coercion]{as.party}} to obtain Kaplan-Meier fits.
}
