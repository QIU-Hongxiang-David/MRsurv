% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util.R
\name{QU.SuperLearner.control}
\alias{QU.SuperLearner.control}
\title{Options of SuperLearner for fitting conditional survival probability functions Q and U}
\usage{
QU.SuperLearner.control(family = gaussian(), SL.library = "SL.lm", ...)
}
\arguments{
\item{family}{family passed to \code{\link[SuperLearner:SuperLearner]{SuperLearner::SuperLearner}}. Defaults to \code{gaussian()}. For compatibility with machine learning algorithms in \code{\link[SuperLearner:SuperLearner]{SuperLearner::SuperLearner}}, \code{family$family} must be "gaussian", with a possibly non-identity link (e.g., \code{gaussian(link=log)}) for more flexible usage of \code{\link[SuperLearner:SL.glm]{SuperLearner::SL.glm}}.}

\item{SL.library}{library of learners passed to \code{\link[SuperLearner:SuperLearner]{SuperLearner::SuperLearner}}. Defaults to linear regression "SL.lm"}

\item{...}{further named arguments passed to \code{\link[SuperLearner:SuperLearner]{SuperLearner::SuperLearner}}}
}
\value{
a \code{QU.SuperLearner.control} object
}
\description{
The output is essentially a list to be passed to \code{\link[SuperLearner:SuperLearner]{SuperLearner::SuperLearner}} by running a command like \code{do.call(SuperLearner, QU.SuperLearner.control)}. The user should not specify \code{Y} and \code{X}.
}
