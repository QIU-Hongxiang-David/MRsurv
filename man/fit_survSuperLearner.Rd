% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_surv.R
\name{fit_survSuperLearner}
\alias{fit_survSuperLearner}
\title{Wrapper of \code{survSuperLearner::survSuperLearner}}
\usage{
fit_survSuperLearner(
  formula,
  data,
  id.var,
  time.var,
  event.var,
  nfold = 2,
  time.grid = NULL,
  time.grid.size = 250,
  option = list(event.SL.library = c("survSL.coxph", "survSL.weibreg", "survSL.gam",
    "survSL.rfsrc"), cens.SL.library = c("survSL.coxph", "survSL.weibreg", "survSL.gam",
    "survSL.rfsrc")),
  cluster.var = NULL,
  obs.weight.var = NULL,
  ...
)
}
\arguments{
\item{formula}{formula containing all covariates to be used}

\item{data}{data containing all covariates, follow-up time, event indicator and id}

\item{id.var}{see \code{\link{MRsurv}}}

\item{time.var}{see \code{\link{MRsurv}}}

\item{event.var}{see \code{\link{MRsurv}}}

\item{nfold}{number of folds used when fitting survival curves with sample splitting. Default is 2. If \code{nfold=1}, sample is not split.}

\item{time.grid}{a vector of time points at which to evaluate the survival curve. If NULL, a default grid will be used.}

\item{time.grid.size}{size of time grid, default to 250. If \code{time.grid} is provided (not NULL), this argument will be ignored.}

\item{option}{a list containing optional arguments passed to \code{\link[survSuperLearner:survSuperLearner]{survSuperLearner::survSuperLearner}}. We encourage using a named list. Will be passed to \code{\link[survSuperLearner:survSuperLearner]{survSuperLearner::survSuperLearner}} by running a command like \code{do.call(survSuperLearner, option)}. The user should not specify \code{time}, \code{event}, \code{X}, or \code{newX}. We encourage the user to specify \code{event.SL.library} and \code{cens.SL.library}.}

\item{cluster.var}{see \code{\link{MRsurv}}. If provided, this variable is used to split samples when using cross-fitting and/or cross-valiation.}

\item{obs.weight.var}{see \code{\link{MRsurv}}}

\item{...}{ignored}
}
\value{
a \code{\link{pred_event_censor}} class containing fitted survival curves for individuals in \code{data}
}
\description{
Wrapper of \code{survSuperLearner::survSuperLearner}
}
